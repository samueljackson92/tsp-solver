sudo: required

virtualenv:
  system_site_packages: true

language: python

python:
  - "2.7"
    
before_install:
  - sudo apt-get install -qq python-numpy python-scipy python-tk

install: "pip install -r requirements.txt"

# command to run tests
script: nosetests
